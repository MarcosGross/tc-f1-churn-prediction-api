# Dependências usadas apenas no CI/testes (não vão para a imagem de produção)
pytest==8.3.3
httpx==0.27.2          # cliente para TestClient do FastAPI
ruff==0.6.9            # linter rápido
pyarrow==17.0.0
